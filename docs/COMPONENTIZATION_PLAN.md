# 播放器与可选在线插件的边界

2026-09-20 阅读体验增量：Pages v8 / SDK 2.12 已完成结构化正文和单层引用，后续共用讨论组件
把详情页、播放器抽屉及旧页面入口的评论／回复状态合一。最新范围见 [插件页面](PLUGIN_PAGES.md)。
平台数据适配仍私有；本轮仅宿主通用呈现调整，不新增 SDK 词汇、账号写入或平台分支。
下一步先验收同一个插件版本在新宿主中的完整只读链路，记录真实账号／Native／DPI 与合成测试的分别结果；
不得把 UI 回归通过当成已安装程序更新或真实平台稳定性。以下版本号和“下一阶段”为历史记录。

2026-09-13 第十四阶段：0.16.12 受控测试候选已构建并以实际签名 MSIX 解包作为冻结宿主，
详见 [验收记录](releases/v0.16.12-acceptance.md)。两组真实旧/新插件分别完成兼容矩阵、离线/UI 交付检查，
正式导入管理器各 64 项升级/恢复演练通过，包含新审阅与 PrepareRecoveryAsync，而不只走旧导入 API。
公开源码在没有私有实现的独立 checkout 构建通过；13 组发布 Web UI、契约 89、计划 41、交付 97、修订 36 项通过。
真实解码合成媒体循环与三轮音视频切换通过；未安装应用、未访问账号、未改变证书信任。
默认临时目录清理断言偶发失败未定因，专用目录三次核心通过不能将它视为修复；QQ 匿名搜索仍返回 ServiceUnavailable。
因此仅进入明确标注限制的预发布测试阶段，不宣称生产交付门禁通过；公有二进制仍需公信签名或明确的测试发布例外授权。

下一阶段（第十五阶段）：保持 SDK 2.10 / Abstractions 1.11 冻结，先定位临时目录清理偶发问题，
补已安装应用选择器/重启/跨屏和真实平台验收，再将新插件修订串入私人 CI 的精确包交付门禁。
既有契约内仅交付插件；出现具体无法表达的功能才提出最小宿主能力变更，另行推进生产签名。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第十三阶段：通用插件管理页的更新差异审阅与“选择旧版包”入口已实现，详见
[PLUGIN_UPDATE_UI.md](PLUGIN_UPDATE_UI.md)。实际导入管理器比较候选与当前选择的版本、能力、页面、
设置键/类型、宿主特性和访问声明；确认前再次验证候选以及审阅基线，变化后必须重新选择并批准。
恢复入口只接收原生选择器选中的同 ID、较低且兼容的包，不保证它曾被安装，不扫描历史私人目录。
继续逐修订信任、导入默认关闭、明确启用和正常重启；不恢复账号/设置，不热替换，不删除收藏引用。
本阶段有一次本体/Host/Web 管理基础能力更新，因此不沿用“649 核心文件不变”的结论；历史冻结目录未改动。
Host SDK 2.10、Abstractions 和平台契约不变，未增加平台分支，既有插件不要求重编。
静态清单兼容不等于开发交付门禁或登录/网络/播放通过；同名声明的业务语义也不是本次差异审计范围。
验证通过：新增审阅/恢复 33 项、核心/Host 回归、原平台 50 项、拆分插件 54 项、契约工具 89 项、
Web 逻辑 27 项、发布输出完整 13 组 UI 及新审阅六组补充复测。Release 构建零警告/错误，核心发布边界通过。
最终纯核心隔离输出 `artifacts/plugin-update-stage13-accepted` 未安装、未上传；Windows 原生选择器、
真实跨屏 DPI/重启和真实平台仍未验收。原生锁释放先于结果回调的时序调整只做编译与源码复查。

下一阶段（第十四阶段）：冻结这个更新后的基础宿主，以实际插件旧/新包串联独立升级验收。
将管理预览、信任确认、取消/错误、启用和下一次会话选择，与页面、搜索、媒体卡片和收藏引用回归关联，
证据绑定精确包与宿主。既有契约内的新功能仍只更新插件；只有超出契约才评估最小基础能力升级。
默认不访问真实账号、不覆盖用户安装、不推送或发布；Native 与真实网络结果继续单独报告。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第十二阶段：候选插件更新计划与隔离回退预检已实现，详见 [PLUGIN_UPDATES.md](PLUGIN_UPDATES.md)。
通用工具绑定精确旧/新压缩包、实际宿主矩阵和交付证据，重新计算清单差异，区分仅插件更新、
需要基础宿主、待验收和拒绝；默认只读输入并生成计划，不执行平台 DLL、不安装、不上传。
可选演练只引用冻结 SDK 的正式导入管理器，验证取消/篡改拒绝、默认关闭、明确启用、新会话选择、
重新导入精确旧包及稳定收藏引用保留。模拟失败不是实际插件崩溃，回退不是自动回滚或活动 DLL 热替换。
41 项计划守卫、36 项矩阵守卫、97 项交付守卫及 89 项契约检查通过；两个实际插件各 55 项隔离演练通过。
两组沿用的历史核心基线共 649 文件不变；默认不演练、旧 SDK、缺证据及混包报告路径均有独立证据。
Release 构建、本地核心/Host、原平台 50 项、拆分插件 54 项及核心发布边界回归通过。
拆分插件回归首次缺少显式 fixture 路径而失败，指定既有隔离平台目录后重跑通过；未读取用户插件目录。
本轮不改本体、Host、Abstractions、Web 协议或平台业务，未做真实网络、账号、声音或安装版 Native 验收。
QQ 此前的 ServiceUnavailable 仍是未解决的真实平台项，离线和更新计划通过不能覆盖它。

下一阶段（第十三阶段）：将更新差异预览与旧包恢复入口接入通用插件管理页。
复用正式导入/逐修订信任/默认关闭/明确启用/重启流程，不加入平台 ID 分支，不执行计划文件中的文案。
先完成版本/兼容性/声明变化的安装前展示与精确旧包重新导入，再做键盘、减少动画、深浅色和 DPI 验收。
这属于通用管理基础能力；如需 Native/Web 协议演进，明确限定一次本体升级，而不要求各平台后续新功能重编本体。
账号写操作、新系统能力与热替换仍独立评估；既有契约内的新页面、查询、设置和媒体卡片继续仅更新插件。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第十一阶段：单插件构建与实际宿主兼容矩阵已实现，详见 [PLUGIN_REVISIONS.md](PLUGIN_REVISIONS.md)。
六个私人插件项目支持 FrozenCore DLL 引用模式，求值 ProjectReference 均为零；入口只构建选定项目，
不重编播放器或其余插件，生成三文件插件包后自动衔接第十阶段门禁。公共工具没有平台 ID 分支。
兼容矩阵实际引用每个旧/新发布 SDK，而非当前源码；分别报告 compatible / incompatible / check-failed，
记录最低 SDK、能力、页面/设置键和凭据/旧设置别名/图片域名的声明差异。哈希不是签名或权限沙箱。
36 项工具守卫与第十阶段 97 项守卫通过；损坏 SDK 实际返回 check-failed，不能伪装成兼容。
QQ 1.15 → 1.16：SDK 2.10 通过；2.8 / 2.2 均明确不兼容，89 项离线 + 6 组 UI 通过。
Bilibili 1.17 → 1.18：SDK 2.10 双版本通过；SDK 2.9 仅旧版通过；2.2 双版本不兼容，
38 项离线 + 6 组 UI 通过，旧展示卡片到新媒体卡片的差异来自实际插件 DLL/清单。
两组固定核心 649 文件不变；矩阵与门禁证据复查通过；无安装、无上传、无真实网络/账号/声音验收。
QQ 最终真实搜索此前的 ServiceUnavailable 未在本阶段修复或重测，线上稳定性仍未通过。

下一阶段（第十二阶段）：候选包更新计划与回退预检。绑定精确旧/新包哈希、兼容矩阵及声明差异，
生成“仅插件更新 / 需要基础 SDK / 拒绝或检查未完成”的明确计划；在隔离目录验证候选版本切换、
失败回退和稳定实体引用的保留，不自动安装用户插件，不迁移账号，不引入更新服务器。
目标是把独立插件交付推进到可审阅、可恢复的更新流程；只有超出既有契约时才提出最小宿主演进。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第十阶段：独立插件交付门禁已实现，详见 [PLUGIN_DELIVERY.md](PLUGIN_DELIVERY.md)。
统一核对新旧真实压缩包与展开文件、冻结发布 SDK、源文件/运行时前后哈希和显式受信任测试配置。
默认仅静态检查；DLL 契约、离线业务与发布 Web UI 分层报告，真实平台 / Native 当前明确为未执行。
结构化结果缺失、旧证据目录、包不匹配、宿主变化或测试失败不能成为“发布可用”。未安装、未上传。
门禁 97 项断言通过；QQ 1.14 → 1.15 / 1.15 → 1.16 分别通过 59 / 89 项离线断言与各 6 组发布 Web UI 检查。
两组固定宿主核心 649 文件、诊断运行时 17 文件不变；默认静态模式也验证为不执行插件代码。
QQ 1.16 上一轮真实搜索 ServiceUnavailable 仍是未解决项，本阶段离线验收不覆盖该失败。

下一阶段（第十一阶段）：单插件构建 + 宿主兼容矩阵。只构建发生变化的插件，不重打包播放器或其余平台；
报告最低 SDK / 新增能力 / 权限声明差异，接入本阶段门禁，并为 Bilibili 增加独立的测试配置。
契约内的新页面/设置/查询/媒体卡片继续通过插件更新交付；超出契约的写权限或系统能力先提出最小宿主演进。
真实平台与已安装应用验收保持显式授权和候选包绑定，不自动更新用户安装，也不以通过脚本代替公开发布批准。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第九阶段：QQ 1.16 的独立发现入口与音乐人搜索已实现，仍冻结 SDK 2.10 / Abstractions 1.11。
GlobalPages / Query / CreatorSearch 与类型化目标连通发现 → 主页 → 作品 / 专辑 / 专辑曲目；不需要先播放歌曲。
入口、10/20 每页选项、查询规则、分页、中英文文案均在插件内，无插件时无入口。
649 核心源码/发布文件、17 诊断运行时文件不变；同一冻结发布 SDK 对照实际 QQ 1.15 / 1.16 DLL，89 项通过。
六组真实发布 Web UI + Native 投影合成数据验收通过，原平台 50/50、拆分插件 54/54；没有安装或发布。
真实匿名链路曾通过搜索 9 位音乐人、主页、20 首作品、20 张专辑和 15 首专辑曲目；修复了 100 人合唱导致整页被拒绝的问题。
但最终包的真实搜索复验返回 ServiceUnavailable，真实分页未完成，因此线上稳定性未通过；不自动借账号或换接口。
上述“实现 / 离线兼容通过”不等于“平台持续可用 / 已安装应用验收通过”。详见 PLUGIN_PAGES.md。

下一阶段（第十阶段）：将独立插件交付验收整理为可重复门禁，继续冻结兼容的基础宿主。
统一旧 / 新真实包、SDK 特性需求、核心哈希清单、隔离信任授权、无插件启动和错误隔离报告。
将结构自检、实际 DLL 契约、功能 / UI 合成回归、真实平台冒烟、Windows 原生验收分开显示，禁止用离线通过覆盖在线失败。
优先把第八 / 九阶段的固定宿主证明接入统一脚本；构建与报告留在本地，默认不联网、不安装、不上传。
QQ 搜索的间歇性上游失败作为真实平台未通过项保留；恢复后再做有界匿名搜索分页验收，不绕过平台限制。
只有新功能超出既有契约（例如账号写入权限或新的系统能力）时，才提出最小宿主升级；不承诺任意能力永久零更新。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第八阶段已完成：冻结 Host SDK 2.10 / Abstractions 1.11，QQ 插件 1.15
仅通过自身实现与清单，把音乐人资料弹层扩展为简介、作品、专辑、专辑曲目的主内容页面。
公共核心源码与已有发布产物共 649 文件哈希/清单保持；同一发布 SDK + 17 文件固定诊断运行时
对照实际 QQ 1.14 / 1.15 DLL，通过 59 项检查。诊断链接未经改动的 Native 源码，非已安装 EXE 验收。
5 次匿名真实请求验证资料、两页作品、专辑列表和专辑曲目；没有账号、取流或真实声音测试。
六组冻结发布 UI 检查覆盖深浅色、1/1.5/2 浏览器缩放、窄窗、空库、键盘、错误恢复、返回与修订。
详见 PLUGIN_PAGES.md；本阶段没有更新本体、签名安装包、GitHub 或用户插件目录。

下一阶段（第九阶段）：继续冻结 SDK 2.10，为 QQ 插件补全独立全局发现入口与音乐人搜索。
复用 GlobalPages、Query、CreatorSearch 和既有音乐人目录，使用户不必先播放一首歌才能进入主页。
入口/筛选/分页/中英文文案由插件拥有，清单缺失时无入口；匿名查询不读取 Cookie、不预取音频。
仍以本体/Host/Abstractions/Web 零改动、旧插件对照、固定宿主测试为门槛。
若现有契约不足，记录具体阻碍后再决定基础宿主演进；点赞/关注/发评论不得伪装成只读导航。
之后把这些固定宿主证明整理为独立插件交付门禁，避免每个插件都临时搭建验收工具。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第七阶段已实现：Pages v6 的归一化作品卡片 → 显式播放 / 加入队列。
Host SDK 2.10 / Abstractions 1.11；仅浏览不取流、不改队列，不给只读 action 播放权限。
同一宿主替换示例插件可新增作品目录并调用既有取流；Bilibili 视频动态已采用通用媒体卡片。
页面持有的作品句柄随插件/账号/音质等上下文修订失效，复用既有单首预缓存与混合队列。

下一阶段优先在冻结 SDK 2.10 的前提下迁移更多真实插件页面（例如音乐人作品/专辑目录），
以“只有插件 DLL/清单发生变化、宿主二进制和 Web 资源哈希不变”为验收门槛。
先复用现有页面、查询、媒体卡片、评论和设置词汇；若确需新词汇，先记录能力缺口，不伪装成插件开关。
后续点赞/关注/评论提交需独立写权限、明确用户意图、确认/错误/撤销设计，不能复用只读 action。
仍不承诺任意新系统能力永远无需升级本体，也不开放任意脚本或活动 DLL 无条件热替换。
用户无需重装本体更新已有契约内功能是目标；旧 SDK 安装版仍需一次基础宿主升级。

以下为历史阶段记录（旧“下一阶段”不覆盖以上当前计划）：

2026-09-13 第六阶段：Pages v5 类型化实体导航已实现（Host SDK 2.9 / Abstractions 1.10）。
同平台的主页面可以声明“作者 / 作品”目标；宿主校验入口、类型、归属和修订，并管理后续标签/分页上下文。
主 WebView 仍只看到 label 和随机句柄；不会生成可播放曲目，也不授予账号写入能力。
Bilibili 搜索作者后的主页、简介、动态和讨论链已迁移；删除了 discover-target 二次包装。
固定宿主双 DLL 验证新增目标及分页已通过，详见 PLUGIN_PAGES.md。

下一阶段建议补齐“页面到播放意图”的独立只读媒体选择/显式播放契约：
由插件声明归一化作品，宿主管理队列、短期租约、权限和预缓存；不得借页面 action 暗中播放。
先以单一作品卡片入口验收，再迁移更多平台。点赞/关注/回复仍需另设写权限与用户确认；
任意 HTML/JS、跨提供方跳转和活动 DLL 热替换仍不开放。本地核心不必外置成空壳。

以下为历史阶段记录：

2026-09-13 第五阶段：Settings v2 分组、双语说明、单选依赖提示与原子条件校验已实现。
Host SDK 2.8 / Abstractions 1.9。固定宿主替换演示插件不仅增加设置，还实际改变插件输出；
QQ 与网关插件采用声明式分组。本轮验证与限制见 [PLUGIN_SETTINGS.md](PLUGIN_SETTINGS.md)。
下一阶段优先统一“作者 / 动态 / 作品”跨页面实体导航与返回上下文，使插件不必自行包装跳转状态。
先定义只读类型化目标、来源归属和失效规则；账号写操作、任意 HTML/JS 与热替换仍不开放。

以下为此前阶段记录：

2026-09-13 第四阶段：Pages v4 声明式只读查询、选择筛选、精确输入校验和查询上下文继承已实现。
Core SDK 2.7 / Abstractions 1.9；同一宿主双插件版本测试覆盖新增表单，而非只把界面硬编码为开关。
本阶段没有账号写入；不是所有旧界面均已迁移。下一阶段优先做声明式设置分组/依赖提示，
并梳理可重用的跨页面实体导航，避免各插件重复封装上下文。任何新契约先补静态自检和无插件回归。
实际验证范围、旧安装包兼容限制和剩余边界以 PLUGIN_PAGES.md 最新阶段记录为准。

2026-09-13 第三阶段：GlobalPages 独立全局上下文、可选侧栏入口、Pages v3 标签分区已实现。
页面读取只在明确导航后触发，标签成功才切换、失败保留旧内容、快速切换取消旧请求。
Core SDK 2.6 / Abstractions 1.8，API 1 和程序集身份保持；旧 Pages 1/2 与社区兼容路径保留。
下述第二阶段“下一阶段”已完成；后续优先声明式输入/筛选、设置分组与受控跨页上下文，
账号写入依然需要单独权限设计。验证范围及限制见 PLUGIN_PAGES.md。

2026-09-13 第二阶段已实现：Pages v2 主内容页、精确作者上下文、稳定卡片、增量分页、
图库及受控讨论入口。复杂动态数据由独立插件组装；旧 CreatorFeed/Pages v1 保留兼容。
下一阶段优先做插件声明的全局导航与多分区页面；可写表单/账号操作需独立权限与确认契约，
不能复用只读 Pages action。当前不宣称任意新控件均无需升级宿主。详见 PLUGIN_PAGES.md。

2026-09-13 新阶段：平台新增只读页面可以由插件声明和返回，核心提供通用 Fluent 渲染。
首阶段已接通媒体/作者入口、卡片/列表、只读导航与失效边界；不是所有旧 UI 已迁移。
后续按增量列表/评论组件 → 复杂页面迁移 → 全局导航/受控表单推进，具体门槛见 [PLUGIN_PAGES.md](PLUGIN_PAGES.md)。

本文是公开核心的当前架构与验收约束，不是私人插件的迁移日志或全部功能外置计划。
**具体在线平台必须独立实现；本地播放器不需要变成等待用户安装组件的空壳。**

## 职责与依赖

| 层 | 负责 | 不应包含 |
| --- | --- | --- |
| Auralis 主程序 | 本地曲库、队列、收藏、播放意图、Fluent 页面、Windows 集成、LAN，以及通用插件管理 | 具体平台 ID 分支、API 端点、账号策略、固定音质/网关字段 |
| Platform.Abstractions | 归一化实体、能力接口、错误、短期媒体租约与插件所需服务契约 | 平台实现、播放器窗口或解码器类型 |
| Platform.Host | 清单发现、兼容校验、明确批准后的惰性加载与能力路由 | 内置平台白名单、历史凭据地址或图片域名回退表 |
| 独立在线插件 | 平台 API、认证/续期、取流、平台歌词/评论/媒体扩展及专属设置声明 | 随默认公开核心分发的私人实现；向主 WebView 暴露秘密 |
| 随包播放、传输、图片组件 | 通用解码、播放会话、HTTP 传输、图片读取及生命周期 | 隐藏在通用组件中的平台专属业务 |

主程序只通过公共平台 Host/契约调用在线能力；Host 不反向依赖主程序或具体平台项目。
播放、传输和图片的具体实现有各自组合点，平台 DLL 不负责替换主窗口或接管队列。
平台负责产出归一化数据；核心验证归属、授权和生命周期后，为界面分配不透明句柄。
插件名称、账号入口、设置、搜索来源和媒体扩展入口依据当前注册及能力声明显示，不按已知平台名称猜测。

本地库、LRC 解析与显示、音效、Windows 媒体控制和 LAN 可以内置。
已有播放/传输组件接口和可选导入功能保持，但继续外置本地功能不是在线解耦的完成条件。
图片工厂接口不等于存在用户可导入的图片插件入口；不要把接口化宣传为全部功能可选安装。

## 生命周期和数据所有权

1. 清单发现只读取元数据，不加载 DLL、不创建平台 HTTP 客户端，也不登录账号。
2. 用户导入并明确批准完整修订；新包默认关闭。启用和更新需要重启，活动会话不热替换 DLL。
3. 首次明确能力调用才激活对应已批准、启用且兼容的插件；缺少必填设置时先引导配置。
4. 停用阻止后续调用并撤销凭据访问；清理使用当前会话修订的批准声明，不能用待更新修订猜测账号地址。
5. 取消、切歌、停用和关闭后的迟到结果不得覆盖新页面、恢复已撤销账号或替换新曲目媒体。

在线收藏保存稳定来源/实体身份和非敏感元数据，不写入本地 `library.json`，不把在线 ID 当文件路径。
插件缺失时保留收藏与回退信息；恢复后按原身份重新解析，不按歌曲标题猜测对应内容。
签名 URL、请求头、Cookie 和 Token 留在后端/隔离登录环境及受控凭据存储，主 WebView 只消费安全投影。
通用媒体调度仍由播放器负责：切歌更新全屏内容，视频与音频共享播放意图和进度，退出或失败释放对应资源。

同进程可信插件不是恶意代码沙箱；SHA-256 校验不能认证发布者，加载上下文也不能保证隔离原生崩溃。
以上是宿主接口和生命周期约束，不代表可安全运行任意不可信 DLL。

## 完成条件与验证证据

公开核心可独立构建只是其中一项，不能据此宣称插件的实际功能保持不变。

| 要求 | 应取得的证据 |
| --- | --- |
| 无平台硬编码与私人依赖 | 审阅主程序及全部随包组件的实现/资源/构建输入；在不含私人源码的独立树构建与发布 |
| 功能由插件声明驱动 | 任意来源 ID、无插件、缺能力/设置、旧包、移除/重启与迟到结果的 Host 和 UI 回归 |
| 管理页可用且一致 | 批量/拖入、错误与取消、默认关闭、启用、重启及清理失败反馈；键盘、深浅色和减少动画 |
| 原有体验保持 | 每个实际插件验证登录/续期、搜索分页、收藏封面、歌词、评论/媒体扩展与音视频切换；只验证其声明的能力 |
| 本地优先与 Windows 行为 | 无在线插件时的本地库/播放、空库、托盘/单实例/媒体键及真实 DPI、窗口模式检查 |
| 可公开交付 | 候选内容、测试数据和素材来源审阅；不带私人 Git 历史、用户状态、私有 DLL 或签名私钥 |

SDK 自检与合成 HTTP/凭据夹具不证明真实账号有效或平台在线；浏览器渲染比例不替代 Windows 跨屏 DPI。
截图能验证可见画面和状态，不能单独证明声音连续性、无双声、音画同步或逐帧动画流畅。
测试账号登录、信任确认及真实凭据删除应由用户参与，不能为了验收自动修改其账号或批准记录。
每次报告须区分已验证、失败、未覆盖及环境阻碍，不能把某个插件或单曲成功扩大为全部通过。

## 贡献与发布

构建和测试入口见仓库 README.md 与 AGENTS.md；插件作者指南见[插件分离说明](PLUGIN_SEPARATION.md)。
设置、凭据、兼容和 UI 的详细约束分别见[声明设置](PLUGIN_SETTINGS.md)、[凭据声明](PLUGIN_CREDENTIALS.md)、
[兼容性](PLUGIN_COMPATIBILITY.md)与[界面规范](UI_AND_INTERACTION.md)。

源代码公开与二进制发行是不同门槛。公开源码仍需素材和依赖审阅；分发解码库的二进制还需核对相应许可、
声明及对应源码要求。自签名测试包不代表受 Windows 信任的生产身份。插件化不豁免平台规则或内容访问权限。
推送仓库、改变可见性、签名或发布安装包需要明确授权，不由导出或自检自动执行。
